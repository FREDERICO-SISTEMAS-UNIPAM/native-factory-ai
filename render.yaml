services:
  - type: web
    name: delivery-radar
    env: node
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: data-storage
      mountPath: /opt/render/project/src/baileys_auth_info
      sizeGB: 1
